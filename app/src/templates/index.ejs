<!DOCTYPE html>
  <head>
    <meta charset="utf-8">

    <title>Ringa Hello World</title>
  </head>
  <body>
    <div class="react-app"></div>

    <!--[if gte IE 9 | !IE ]><!-->
    <% for (var chunk in htmlWebpackPlugin.files.chunks) { %>
      <script src="<%= htmlWebpackPlugin.files.chunks[chunk].entry %>"></script>
    <% } %>
    <![endif]-->
  </body>
</html>